name: Deploy Protected Sauna Page

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Insert API Key
        run: |
          # Először behelyettesítjük az API kulcsot a fájlba
          sed -i "s/REPLACE_ME/${{ secrets.THINGSPEAK_KEY }}/g" index.html

      - name: Encrypt index.html with Staticrypt
        env:
          PASSWORD: ${{ secrets.WEB_PASSWORD }}
        run: |
          # 1. Titkosítunk egy ÚJ fájlba (secret.html)
          npx staticrypt index.html -p "$PASSWORD" --shorten -o secret.html
          
          # 2. Megnézzük, létrejött-e (biztonsági ellenőrzés a naplóban)
          ls -l secret.html
          
          # 3. Kicseréljük az eredeti index.html-t a titkosítottra
          mv secret.html index.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
